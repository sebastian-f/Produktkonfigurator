@model List<ProductConfigurator.WebUI.Models.PartViewModel>


@using(Html.BeginForm())
{
	foreach (var item in Model)
	{
		@item.Name
	
		<input class="box" type="checkbox" checked="@item.Checked" value="@item.Id" />
		<br />
	}
}